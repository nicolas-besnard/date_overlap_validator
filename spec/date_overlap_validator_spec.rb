require 'spec_helper'

describe DateOverlapValidator do
  it 'has a version number' do
    expect(DateOverlapValidator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
